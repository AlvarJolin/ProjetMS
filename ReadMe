IMPORTANT
The batch file must be executed to add the modules if you don't have them BEFORE executing the scripts as they are not using only native python but also databases from google and maybe other modules
Otherwise an error will appear